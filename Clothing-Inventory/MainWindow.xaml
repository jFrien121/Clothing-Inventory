<Window x:Class="Clothing_Inventory.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Clothing_Inventory" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Top-Inventory-1.0" Height="450" Width="800">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="0.15*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <materialDesign:ColorZone 
            Grid.Row="0"
            Padding="16"
            Mode="PrimaryMid">
            <StackPanel Orientation="Horizontal">
                <materialDesign:PackIcon Kind="Hanger" />
                <TextBlock
                    Margin="16,0,0,0"
                    VerticalAlignment="Center"
                    Text="Clothing Inventory" />
            </StackPanel>
        </materialDesign:ColorZone>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width=".50*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="0.3*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <DataGrid Grid.Row="1"
                    Loaded="DataGrid_Loaded">
                    <!--<DataGridTextColumn Header="Last Worn"/>
                    <DataGridTextColumn Header="Description"/>
                    <DataGridTextColumn Header="Colour(s)"/>
                    <DataGridTextColumn Header="Shirt Type"/>-->
                </DataGrid>
            </Grid>
        </Grid>
    </Grid>
</Window>
